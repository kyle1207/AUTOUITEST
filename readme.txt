1. pip install pytest
2. pip install pywinauto
3. pip install selenium
4. pip install pytest-html
5. pip install PyTestReport
6. pip install pytest-ordering
5. pip install pytest-rerunfailures(optional) 失败重跑
6. python -m pip install xdist(not used) 并发

7.  test  pull